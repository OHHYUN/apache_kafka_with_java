plugins {
    id 'java'
}

group 'com.pipeline'
version '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.apache.kafka:kafka-streams:2.5.0'
    compile 'com.google.code.gson:gson:2.8.0'
}

test {
    useJUnitPlatform()
}